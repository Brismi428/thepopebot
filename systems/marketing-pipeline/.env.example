# Marketing Pipeline — Environment Variables
# Copy this file to .env and fill in your actual API keys.
# NEVER commit .env to version control.

# Required: Claude Code execution, email generation, pain signal analysis
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Apollo.io for company enrichment and contact discovery
# Without this, the system falls back to Brave Search + Claude (reduced data quality)
APOLLO_API_KEY=your_apollo_api_key_here

# Optional: Hunter.io for email verification and discovery
# Without this, the system uses Apollo contacts only
HUNTER_API_KEY=your_hunter_api_key_here

# Optional: Brave Search for company research (blog posts, job listings, funding)
# Without this, research-based enrichment is skipped
BRAVE_API_KEY=your_brave_api_key_here

# Optional: Enable Agent Teams for parallel outreach/nurture generation
# Set to 1 to enable, remove or set to 0 to disable
# CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
